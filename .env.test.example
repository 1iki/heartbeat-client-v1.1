# Example Environment Variables for Testing
# Copy this to .env.test and fill in your actual values
# DO NOT commit .env.test to git!

# Target URL to monitor (the page after login)
TEST_URL=https://example.com/dashboard

# Login page URL (optional, defaults to TEST_URL if not set)
LOGIN_URL=https://example.com/login

# Login credentials
TEST_USERNAME=your-username-here
TEST_PASSWORD=your-password-here

# API URL (optional, defaults to http://localhost:3000)
API_URL=http://localhost:3000

# Advanced: Custom selectors (optional, for auto-detection failure)
# TEST_USERNAME_SELECTOR=#email
# TEST_PASSWORD_SELECTOR=#password
# TEST_SUBMIT_SELECTOR=button[type="submit"]
# Konfigurasi Logging & Health Check
LOG_LEVEL=INFO
HEALTH_CHECK_INTERVAL=30000
HEALTH_CHECK_TIMEOUT=10000